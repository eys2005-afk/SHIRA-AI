# העתק את הקובץ הזה ל-.env (באותה תיקייה של shira_proxy.py) ומלא את המפתח האמיתי.
# הקובץ .env לא נשלח ל-git (ראה .gitignore).

# מפתח ה-API של Gemini — https://aistudio.google.com/apikey
GEMINI_API_KEY=
